# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Lint, Format and Unit Test Validation
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  pre-commit:
    name: Pre-Commit Lint and Validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Bootstrap GEM Environment
        uses: ./.github/actions/setup-gem

      - name: Check for changes
        id: changes
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        with:
          filters: |
            terraform:
              - 'terraform/**'
            ansible:
              - 'ansible/**'
            golang:
              - 'operators/**'
              - '**/*.go'
              - '**/go.mod'
              - '**/go.sum'
              - '.golangci.yml'

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'operators/gem-network-operator/go.mod'

      - name: Setup TFLint
        if: steps.changes.outputs.terraform == 'true'
        uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a # v6.3.0
        with:
          tflint_version: v0.50.0

      - name: Run Linters and Validators
        env:
          SKIP: terraform_fmt,terraform_tflint,terraform_validate,ansible-lint,golangci-lint
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1

      - name: Run Golang Linters
        if: steps.changes.outputs.golang == 'true'
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: v2.12.2
          working-directory: operators/gem-network-operator
          args: --timeout=5m --config=../../.golangci.yml

      - name: Terraform Init Mocks
        if: steps.changes.outputs.terraform == 'true'
        run: |
          for dir in terraform/bootstrap terraform/cluster; do
            if [ -d "$dir" ]; then
              echo "Initializing $dir..."
              backend_file="$dir/backend.tf"
              [ -f "$backend_file" ] && mv "$backend_file" "${backend_file}.bak"
              terraform -chdir="$dir" init -backend=false
              [ -f "${backend_file}.bak" ] && mv "${backend_file}.bak" "$backend_file"
            fi
          done
        shell: bash

      - name: Run Terraform Pre-Commit Checks
        if: steps.changes.outputs.terraform == 'true'
        env:
          SKIP: trailing-whitespace,end-of-file-fixer,check-yaml,check-added-large-files,yamlfmt,gitleaks,addlicense,shellcheck,ansible-lint,golangci-lint
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1

      - name: Run Ansible Pre-Commit Checks
        if: steps.changes.outputs.ansible == 'true'
        env:
          SKIP: trailing-whitespace,end-of-file-fixer,check-yaml,check-added-large-files,yamlfmt,gitleaks,addlicense,shellcheck,terraform_fmt,terraform_tflint,terraform_validate,golangci-lint
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1

  unit-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Bootstrap GEM Environment
        uses: ./.github/actions/setup-gem

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'operators/gem-network-operator/go.mod'

      - name: Run GEM Unit Tests
        run: |
          chmod +x ./scripts/run-unit-tests.sh
          ./scripts/run-unit-tests.sh
        shell: bash
