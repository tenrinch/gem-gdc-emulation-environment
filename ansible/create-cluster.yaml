# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
- name: Wait dynamically for GCE Guest Agent metadata synchronization
  hosts: cluster_nodes
  gather_facts: false
  tasks:
    - name: Poll SSH until public key is accepted
      ansible.builtin.wait_for_connection:
        delay: 0
        sleep: 5
        timeout: 120

- name: Ensure GCP SSH Key exists locally
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    gcp_project_id: "{{ gcp_project_id | default(lookup('env', 'PROJECT_ID'), true) }}"
    gcp_zone: "{{ gcp_zone | default(lookup('env', 'GEM_GCP_ZONE'), true) }}"
  tasks:

    - name: Assert CLUSTER_NAME environment variable is set
      ansible.builtin.assert:
        that:
          - lookup('env', 'CLUSTER_NAME') != ""
        fail_msg: "🚫 ERROR: You must set the CLUSTER_NAME environment variable before running this playbook (e.g., export CLUSTER_NAME=gem-cluster-1)"
    - name: Assert CLUSTER_NAME does not exceed Kubernetes FQDN character limits
      ansible.builtin.assert:
        that:
          - (lookup('env', 'CLUSTER_NAME') | length) + (gcp_zone | length) + (gcp_project_id | length) + 15 <= 63
        fail_msg: >-
          🚫 ERROR: CLUSTER_NAME '{{ lookup('env', 'CLUSTER_NAME') }}' is too long
          for project '{{ gcp_project_id }}' and zone '{{ gcp_zone }}'.
          Combined node FQDNs would exceed Kubernetes' strict 63-character metadata label limit.
          Maximum allowed CLUSTER_NAME length is {{ 63 - (gcp_zone | length) - (gcp_project_id | length) - 15 }} characters.
    - name: Check if google_compute_engine key exists
      ansible.builtin.stat:
        path: "~/.ssh/google_compute_engine"
      register: gcp_ssh_key
    - name: Generate google_compute_engine key if missing
      ansible.builtin.command: gcloud compute config-ssh --quiet --project={{ gcp_project_id }}
      when: not gcp_ssh_key.stat.exists
      changed_when: true
- name: Fetch SSH Key from Workstation
  hosts: workstation
  become: true
  tasks:
    - name: Fetch platform user public key from workstation
      ansible.builtin.fetch:
        src: "{{ gem_home }}/.ssh/id_rsa.pub"
        dest: /tmp/ws_id_rsa.pub
        flat: true
- name: Setup SSH Access and Base OS on Cluster Nodes
  hosts: cluster_nodes
  become: true
  any_errors_fatal: true
  roles:
    - cluster_nodes

- name: Authorize platform SSH key on Edge Router
  hosts: edge_router
  become: true
  ignore_unreachable: true
  tasks:
    - name: Ensure .ssh directory exists
      ansible.builtin.file:
        path: "{{ gem_home }}/.ssh"
        state: directory
        owner: "{{ gem_user }}"
        group: "{{ gem_user }}"
        mode: "0700"
    - name: Authorize Workstation Public Key on Edge Router
      ansible.posix.authorized_key:
        user: "{{ gem_user }}"
        key: "{{ lookup('file', '/tmp/ws_id_rsa.pub') }}"
        state: present
      when: lookup('file', '/tmp/ws_id_rsa.pub', errors='ignore') != ""

- name: Setup VXLAN on all GCE instances
  hosts: gdc_nodes
  become: true
  vars:
    cluster_name: "{{ tf_cluster_name }}"
  roles:
    - vxlan
- name: Deploy Google Distributed Cloud Hybrid Cluster
  hosts: workstation
  become: true
  vars:
    cluster_name: "{{ tf_cluster_name }}"
  roles:
    - gdc_deploy
    - topolvm
    - gatekeeper
    - secondary_networks
    - gvisor
- name: Configure gVisor container runtime on cluster nodes
  hosts: cluster_nodes
  become: true
  roles:
    - gvisor_node
